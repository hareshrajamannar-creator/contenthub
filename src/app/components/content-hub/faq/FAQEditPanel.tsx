/**
 * @deprecated — retired. The FAQ right edit panel is now `EditFAQPanel` defined
 * inline in CreateView.tsx (lines ~1733+). It uses the faqStore for live updates
 * and the shared ContentScorePanel for score display.
 *
 * This stub is kept only because the mounted filesystem does not allow deletion.
 * Do not import from here.
 */

export type { } from './faqStore';
